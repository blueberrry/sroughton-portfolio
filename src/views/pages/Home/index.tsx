import React from 'react';

import { Section } from 'src/components/Layouts';

import './index.scss';

function Home() {
  // return <Article type='primary'>test</Article>;

  return (
    <Section type='primary' title="‘We’re in a new era’: the 21st-century space race takes off'">
      fekfef
    </Section>
  );
}

// render alongside desktop/ route to on mobile

export default Home;
